---
title: "LLM"
order: 10
---

- Opens shallow: large language models generate text by predicting the next word. Introduces [foundation model](#foundation-model) and [parameters](#parameters) in passing — just enough to name them.
- Then the headings below restate and deepen each concept.

## Foundation model

- The pretrained base model before any fine-tuning. What it means, why it's the starting point for everything downstream.

## Parameters

- The numbers inside the model that get adjusted during training. What "7B parameters" means. Why size matters.
